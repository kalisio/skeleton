import{_ as e,c as a,o as s,V as i}from"./chunks/framework.Q5AHJ25T.js";const k=JSON.parse('{"title":"Publish your app","description":"","frontmatter":{},"headers":[],"relativePath":"guides/development/publish.md","filePath":"guides/development/publish.md"}'),t={name:"guides/development/publish.md"},o=i(`<h1 id="publish-your-app" tabindex="-1">Publish your app <a class="header-anchor" href="#publish-your-app" aria-label="Permalink to &quot;Publish your app&quot;">​</a></h1><p>In a nutshell, the tasks are performed manually and independently using the required commands at the different stages of the application lifecycle to be published in the Docker Hub.</p><p>In order to change the version number in a web app you have to increase the package version number in the <strong>package.json</strong> file (frontend and backend API), and possibly Cordova configuration file, of your branch so that the generated artefacts will not erase previously published ones. Depending on the release typing the following command will do the job (where type is either <code>patch</code>, <code>minor</code>, <code>major</code>):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run release:type</span></span></code></pre></div><p>Usually, you start a new version by creating a <code>test</code> (a.k.a release or staging) branch from your <code>master</code> branch. You should then increase the version number (major or minor) of your <code>master</code> branch so that the generated artefacts of the new development version will not erase previously published ones, e.g. <code>npm run release:major</code>.</p><p>When you are satisfied enough with your version you typically release it starting from your <code>test</code> (a.k.a release or staging) branch by creating a tag accordingly in the git repository and push it. This process usually triggers your <a href="./deploy.html">CI/CD</a> process to build the target artefacts.</p><p>Then, you should increase the version patch number of your <code>test</code> branch so that the generated artefacts of the new staging version will not erase previously published ones, e.g. <code>npm run release:patch</code>. Indeed, a staging branch should only include patch versions not major/minor versions.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Before you publish your app take care of updating the version of all dependent modules to the latest version published, for example perform <code>yarn upgrade kdk</code> to use the latest versin of the KDK.</p></div><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>If you are using our <a href="./../../tools/cli.html#kdk-cli">CLI</a> take care of updating/creating the required workspace file before you publish your app.</p></div><h3 id="manual-build" tabindex="-1">Manual build <a class="header-anchor" href="#manual-build" aria-label="Permalink to &quot;Manual build&quot;">​</a></h3><p>Because web app are released as Docker images you can build it locally for testing purpose like this:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build -t kalisio/kapp .</span></span></code></pre></div><p>Then release it as latest version:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> login</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push kalisio/kapp</span></span></code></pre></div><p>And tag it (<code>version_tag</code> being the current version number like <code>1.1.2-prod</code> or <code>1.1.0-dev</code> depending on the <a href="./deploy.html">flavor</a>)</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tag kalisio/kapp kalisio/kapp:version_tag</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push kalisio/kapp:version_tag</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>This requires you to have a DockerHub account and be a team member of the Kalisio organization, if you&#39;d like to become a maintainer please tell us</p></div>`,17),n=[o];function l(r,p,h,d,c,u){return s(),a("div",null,n)}const b=e(t,[["render",l]]);export{k as __pageData,b as default};
